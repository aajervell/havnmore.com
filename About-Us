<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>About - havnmore</title>
    <!-- Tailwind (optional, if you were using it in your React project) -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- React, ReactDOM and Babel (for running JSX directly in the browser) -->
    <script src="https://unpkg.com/react@18/umd/react.development.js" crossorigin></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js" crossorigin></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>

    <!-- React Router and lucide-react are not directly usable via plain HTML UMD in the same way.
         In a real project you’d typically bundle this with a build tool (Vite, CRA, Next, etc.). [web:16][web:18]
    -->
  </head>
  <body class="bg-white">
    <div id="root"></div>

    <script type="text/babel">
      import React from 'react';
      import { ArrowRight, Target, Users, Zap, Heart, Code, Sparkles } from 'lucide-react';
      import { Link } from 'react-router-dom';
      import { createPageUrl } from '../utils';

      export default function About() {
        return (
          <div className="bg-white text-gray-800 font-light min-h-screen">
            {/* Navigation */}
            <nav className="container mx-auto px-6 py-6">
              <div className="flex items-center justify-between">
                <Link to={createPageUrl('Home')} className="flex items-center gap-2">
                  <div className="h-10 w-10 rounded-lg bg-gradient-to-br from-gray-900 to-black flex items-center justify-center">
                    <span className="text-white font-bold text-xl">H</span>
                  </div>
                  <span className="text-2xl font-light tracking-tight">havnmore</span>
                </Link>
                <div className="hidden md:flex space-x-8 text-sm text-gray-600">
                  <Link to={createPageUrl('Home')} className="hover:text-black transition-colors">Services</Link>
                  <Link to={createPageUrl('Home')} className="hover:text-black transition-colors">Work</Link>
                  <Link to={createPageUrl('About')} className="hover:text-black transition-colors">About Us</Link>
                </div>
                <div className="flex items-center gap-4">
                  <a href="https://form.typeform.com/to/U9C6k2dX" target="_blank" rel="noopener noreferrer" className="bg-gradient-to-r from-gray-900 to-black text-white text-sm rounded-full px-6 py-2.5 hover:shadow-lg hover:scale-105 transition-all">
                    Start Project
                  </a>
                </div>
              </div>
            </nav>

            <div className="h-px bg-gradient-to-r from-transparent via-gray-200 to-transparent"></div>

            {/* Hero Section */}
            <div className="relative overflow-hidden">
              <div className="absolute inset-0 bg-gradient-to-br from-gray-50 via-white to-gray-100 z-0"></div>
              
              <div className="container mx-auto px-6 py-20 md:py-32 relative z-10">
                <div className="max-w-4xl mx-auto text-center">
                  <div className="inline-block mb-6">
                    <span className="text-sm font-medium text-gray-900 bg-gray-100 px-4 py-2 rounded-full border border-gray-200">
                      About havnmore
                    </span>
                  </div>
                  <h1 className="text-5xl md:text-6xl lg:text-7xl font-light tracking-tight leading-tight mb-8">
                    We help businesses <span className="bg-clip-text text-transparent bg-gradient-to-r from-gray-900 via-gray-700 to-gray-900">reach new customers</span> online
                  </h1>
                  <p className="text-gray-600 text-xl md:text-2xl font-light leading-relaxed mb-12">
                    In today's digital world, your website is often the first impression customers have of your business. We specialize in creating websites that don't just look great—they drive real results.
                  </p>
                </div>
              </div>
            </div>

            {/* Mission Section */}
            <div className="container mx-auto px-6 py-20">
              <div className="max-w-4xl mx-auto">
                <div className="grid md:grid-cols-2 gap-12 mb-20">
                  <div>
                    <div className="h-16 w-16 rounded-2xl bg-gradient-to-br from-gray-800 to-black flex items-center justify-center mb-6 shadow-lg">
                      <Target className="w-8 h-8 text-white" />
                    </div>
                    <h2 className="text-3xl font-light mb-4">Our Mission</h2>
                    <p className="text-gray-600 font-light leading-relaxed">
                      We believe every business deserves a powerful online presence. Our mission is to make professional web design accessible to startups, small businesses, and entrepreneurs who want to grow but don't have the budget for expensive agencies.
                    </p>
                  </div>
                  
                  <div>
                    <div className="h-16 w-16 rounded-2xl bg-gradient-to-br from-gray-700 to-gray-900 flex items-center justify-center mb-6 shadow-lg">
                      <Heart className="w-8 h-8 text-white" />
                    </div>
                    <h2 className="text-3xl font-light mb-4">Our Approach</h2>
                    <p className="text-gray-600 font-light leading-relaxed">
                      We're not just
